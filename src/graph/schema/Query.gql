type Query {
  drivers(filter: DriverFilter, limit: Int): [Driver]
  driver(id: String): Driver
  driverSeasons: [DriverSeason]
  teams(name: String): [Team]
  teamSeasons: [TeamSeason]
  countries(name: String): [Country]
  factories: [Factory]
  circuits(filter: CircuitFilter, limit: Int): [Circuit]
  cars(filter: CarFilter, limit: Int): [Car]
  seasons(year: Int): [Season]
  races(name: String): [Race]
}
